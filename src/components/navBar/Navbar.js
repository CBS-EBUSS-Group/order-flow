import React from "react";
import styles from "./Navbar.module.css";

const NavBar = () => {
  return (
    <div className={styles.nav}>
      <h2 className={styles.title}>Order FLow</h2>
    </div>
  );
};

export default NavBar;
